NOTE: Before running the server, create ```.env``` file and write your ```Database credentials``` for the server to run without crashing.
```
MONGO_USER=<userName>
  ```
  ```
MONGO_PASSWORD=<password>
```